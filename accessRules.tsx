import { User } from './types';

// Arquivo desativado conforme solicitação.
// Exportação mantida para evitar erros de compilação
export const canAccessPerformanceMonitor = (user: User | null): boolean => {
  return true;
};
